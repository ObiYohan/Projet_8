name: CI/CD Pipeline

on:
  push:
    branches: [ master, dev ]
  pull_request:
    branches: [ master ]

env:
  DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/credit-risk-api
  HF_SPACE: ${{ secrets.HF_USERNAME }}/credit-risk-api

jobs:
  # Job 1: Tests
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml') }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .
        pip install pytest pytest-cov

    - name: Run unit tests
      run: |
        pytest tests/ -v --cov=api --cov-report=xml --cov-report=term
    
    - name: Upload coverage reports
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella

  # Job 2: Build Docker Image
#   build:
#     name: Build Docker Image
#     needs: test
#     runs-on: ubuntu-latest
#     if: github.event_name == 'push'
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
    
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}
    
#     - name: Build and push Docker image
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         push: true
#         tags: |
#           ${{ env.DOCKER_IMAGE }}:latest
#           ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
#         cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
#         cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

  # Job 3: Deploy to Hugging Face Spaces
#   deploy:
#     name: Deploy to Hugging Face Spaces
#     needs: build
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main'
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Push to Hugging Face Spaces
#       env:
#         HF_TOKEN: ${{ secrets.HF_TOKEN }}
#       run: |
#         git config --global user.email "github-actions@github.com"
#         git config --global user.name "GitHub Actions"
        
#         # Clone HF Space
#         git clone https://huggingface.co/spaces/${{ env.HF_SPACE }} hf-space
        
#         # Copy files
#         cp Dockerfile hf-space/
#         cp requirements.txt hf-space/
#         cp -r api/ hf-space/
#         cp -r models/ hf-space/
#         cp README.md hf-space/
        
#         # Commit and push
#         cd hf-space
#         git add .
#         git commit -m "Deploy from GitHub Actions - ${{ github.sha }}" || echo "No changes"
#         git push https://user:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/${{ env.HF_SPACE }}